<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/nav_home">

    <fragment
        android:id="@+id/nav_home"
        android:name="com.example.tasks.view.AllTasksFragment"
        android:label="Tarefas"
        tools:layout="@layout/fragment_all_tasks" />

    <fragment
        android:id="@+id/nav_gallery"
        android:name="com.example.tasks.view.NextTasksFragment"
        android:label="Próximos 7 dias"
        tools:layout="@layout/fragment_next_tasks" />

    <fragment
        android:id="@+id/nav_slideshow"
        android:name="com.example.tasks.view.ExpiredTasksFragment"
        android:label="Expiradas"
        tools:layout="@layout/fragment_expired_tasks" />
</navigation>